import java.util.Scanner;
public class ReadNumber {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int number;
        System.out.println("Type a number above 0 and under 1000: ");
        number = sc.nextInt();
        int hundred = Math.round(number/ 100);
        int ten = Math.round(number );
        int numberDiv1 = numberDiv10 / 1;
        switch (number) {
        }
    }
}
